const state = {
  songs: []
}

const actions = {
  login({ commit, state }) {
  }
}

const mutations = {}

const getters = {}

const user = {
  namespaced: true,
  actions,
  state,
  mutations,
  getters
}
export default user
